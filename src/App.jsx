import React from 'react';
// import PropTypes from 'prop-types';

import GameBoard from './containers/GameBoard';

const App = () => (
  <main>
    <h1>Game of Life</h1>
    <GameBoard />
  </main>
    );

export default App;
